import React from "react";
import HomeHeader from "./homeHeader";
import Layout from "../utils/layout";

function HomePage() {
    return (
        <Layout>
            <HomeHeader name="Pietro"/>
        </Layout>
    );
}

export default HomePage;